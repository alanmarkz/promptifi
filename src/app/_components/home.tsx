import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div className="w-full h-screen flex items-center justify-center">
      <div>
        <input placeholder="hext" className="bg-gray-700"></input>
      </div>
    </div>
  );
};

export default Home;
